language: python

python:
  - "2.7"

# command to install dependencies
install:
  - "pip install -r requirements.txt"

# # set permissions
# before_script:
#  - chmod a+x ./ticker.py

# command to run tests
script:
  - python ticker.py --test

# # turn off email notifications
# notifications:
#     email: false
